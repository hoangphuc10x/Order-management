name: CI/CD - Build & Deploy

# Chạy khi push lên nhánh main (hoặc khi mở PR vào main để kiểm tra trước)
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  # 1) Build thử SERVER — bắt lỗi TypeScript trước khi deploy
  build-server:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: server
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: server/package-lock.json
      - run: npm ci
      - run: npm run build

  # 2) Build thử CLIENT — bắt lỗi TS/Vite trước khi deploy
  build-client:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: client
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: client/package-lock.json
      - run: npm ci
      - run: npm run build

  # 3) Cả 2 build pass + là push lên main -> kích hoạt deploy
  deploy:
    needs: [build-server, build-client]
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    steps:
      - name: Deploy server len Render
        env:
          RENDER_DEPLOY_HOOK: ${{ secrets.RENDER_DEPLOY_HOOK }}
        run: |
          if [ -z "$RENDER_DEPLOY_HOOK" ]; then
            echo "::warning::RENDER_DEPLOY_HOOK chua duoc cau hinh -> bo qua deploy server."
            exit 0
          fi
          curl -fsS -X POST "$RENDER_DEPLOY_HOOK"

      - name: Deploy client len Vercel
        env:
          VERCEL_DEPLOY_HOOK: ${{ secrets.VERCEL_DEPLOY_HOOK }}
        run: |
          if [ -z "$VERCEL_DEPLOY_HOOK" ]; then
            echo "::warning::VERCEL_DEPLOY_HOOK chua duoc cau hinh -> bo qua deploy client."
            exit 0
          fi
          curl -fsS -X POST "$VERCEL_DEPLOY_HOOK"
